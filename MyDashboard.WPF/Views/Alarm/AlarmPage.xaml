<UserControl x:Class="MyDashboard.WPF.Views.Alarm.AlarmPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MyDashboard.WPF.ViewModels"
             mc:Ignorable="d">
    <!-- DataContext will be set in code-behind using dependency injection -->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Filter bar -->
        <Grid Grid.Row="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <ComboBox Grid.Column="0" Width="100" ItemsSource="{Binding Lines}" SelectedItem="{Binding SelectedLine}" Margin="5" Style="{StaticResource ModernComboBoxStyle}"/>
            <DatePicker Grid.Column="1" SelectedDate="{Binding FromDate}" Width="150" Margin="5" Style="{StaticResource ModernDatePickerStyle}"/>
            <DatePicker Grid.Column="2" SelectedDate="{Binding ToDate}" Width="150" Margin="5" Style="{StaticResource ModernDatePickerStyle}"/>
            <TextBox Grid.Column="3" Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="5" Style="{StaticResource ModernTextBoxStyle}"/>
            <Button Grid.Column="4" Command="{Binding SearchCommand}" Width="100" Margin="5" Style="{StaticResource PrimaryButtonStyle}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Search"/>
                </StackPanel>
            </Button>
            <Button Grid.Column="5" Command="{Binding ClearFiltersCommand}" Width="120" Margin="5" Style="{StaticResource SecondaryButtonStyle}">
                <TextBlock Text="Clear Filters"/>
            </Button>
        </Grid>

        <!-- Table -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Alarms}" AutoGenerateColumns="False" Margin="10" Style="{StaticResource ModernDataGridStyle}" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date/Time" Binding="{Binding DateTime, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="150"/>
                <DataGridTextColumn Header="Line" Binding="{Binding Line}" Width="80"/>
                <DataGridTextColumn Header="Alarm Message" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
